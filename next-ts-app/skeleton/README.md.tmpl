# ${{ values.name }}

${{ values.description }}

A Next.js App Router app in TypeScript, scaffolded by the platform self-service catalog.

## Develop

```bash
npm install
npm run dev
```

The app runs on http://localhost:3000 with a health check at `/api/health`.

## Build and run

```bash
npm run build
npm start
```
